var TodoModel = require ('../models/bree.model.js');

/* para cada funcionalidad según lo requerido

module.exports.save = function(req,res){
  BreeModel.create(req.body).then(function(obj)){
	if(obj){
	res.send({status : 201, message : 'Success'});
}else if(!obj){
   res.send({status: 401, message : 'Fail'});
}
});
};
*/
