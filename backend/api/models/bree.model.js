var mongoose = require('mongoose');

var breeSchema = mongoose.Schema({
    //atributo : tipo,
		//atributo : tipo,
});

/*'Bree '= nombre de la tabla, mongoose por defecto la pluraliza y la pone en lowercase , siempre poner el nombre de
 la tabla en mayuscula y en singular
cambiar el nombre de la tabla*/

//module.exports = mongoose.model('Bree', breeSchema);
